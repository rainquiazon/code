// Make an object with car details
let car = {
    type: "montero",
    model: "2024",
    color: "Black"
};

// Show what type of data 'car' is
console.log(typeof car);

// Change car type to "Toyota"
car.type = "mitsubishi";
console.log(car);

// Add a new detail: the car has 4 wheels
car.wheels = 4;
console.log(car);
